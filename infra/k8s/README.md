# Kubernetes

Place workload manifests here (`Deployment`, `Service`, `Ingress`, `ConfigMap`, `Secret`).

Treat secrets via your cluster mechanism (external secrets operator, sealed secrets, cloud KMS), not plaintext in Git.
