name: ecommerce_analytics
version: '1.0'
config-version: 2

profile: ecommerce_analytics

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  ecommerce_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
